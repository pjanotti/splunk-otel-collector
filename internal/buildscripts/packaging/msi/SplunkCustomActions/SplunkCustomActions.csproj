<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <OutputType>Library</OutputType>
    <RootNamespace>SplunkCustomActions</RootNamespace>
    <TargetFramework>net462</TargetFramework>
    <LangVersion>Latest</LangVersion>
    <WixNuGetPackageRoot Condition="'$(OS)' != 'Windows_NT'">$(HOME)/.nuget/packages/wix/3.14.0</WixNuGetPackageRoot>
    <WixNuGetPackageRoot Condition="'$(OS)' == 'Windows_NT'">$(APPDATA)/NuGet/packages/wix/3.14.0</WixNuGetPackageRoot>
  </PropertyGroup>

  <ItemGroup Condition=" '$(TargetFrameworkIdentifier)' == '.NETFramework' ">
    <Using Remove="System.Net.Http" />
  </ItemGroup>
  
  <ItemGroup>
    <!-- This doesn't directly bring the dependency needed to build the project but installs it on the NuGet cache -->
    <PackageReference Include="wix" Version="3.14" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Microsoft.Deployment.WindowsInstaller">
      <HintPath>$(WixNuGetPackageRoot)/tools/sdk/Microsoft.Deployment.WindowsInstaller.dll</HintPath>
    </Reference>
  </ItemGroup>

</Project>
